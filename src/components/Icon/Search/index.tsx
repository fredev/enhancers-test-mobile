import React from 'react';
import {Image} from 'react-native';
import search from './search.png';

const Search: React.FC = () => <Image source={search} style={{height: 28}} />;

export default Search;
